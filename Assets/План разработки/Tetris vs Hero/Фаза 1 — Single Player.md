# 🕹 Фаза 1 — Single Player

> [!tip] Статус — в процессе
> Все 12 скриптов написаны. Осталось собрать сцену в Unity и проверить в игре.

---

## Шаг 1 — Создать сцену

### GameObjects

Создать **пустые GameObject'ы** и прикрепить компоненты:

| # | GameObject | Компоненты |
|---|------------|------------|
| 1 | `GameManager` | `GameManager` |
| 2 | `TetrisController` | `TetrisController` |
| 3 | `BoardRenderer` | `BoardRenderer` |
| 4 | `BlockInteraction` | `BlockInteraction` |
| 5 | `Character` | `CharacterController2D` · `CharacterView` · `SpriteRenderer` · `Animator` |
| 6 | `Canvas` | `Canvas` · `CanvasScaler` · `GameUI` |

### Ссылки в Inspector → GameManager

| Поле | Назначить |
|------|-----------|
| TetrisController | → `TetrisController` |
| BoardRenderer | → `BoardRenderer` |
| CharacterController2D | → `Character` |
| BlockInteraction | → `BlockInteraction` |
| GameUI | → `Canvas` |
| BlocksToWin | `30` |

> [!warning] Порядок инициализации
> `GameManager.Awake()` вызывает `Init()` по цепочке.
> Все ссылки в Inspector должны быть заполнены **до** первого запуска.

---

## Шаг 2 — Арт (заглушки)

| Элемент | Как сделать |
|---------|-------------|
| Ячейка доски | `Assets → Create → Sprites → Square` → назначить в `BoardRenderer._cellSprite` |
| Персонаж | Цветной прямоугольник `~0.8 × 1.8` в `SpriteRenderer` |
| Фон | Цвет камеры `Background: #1a1a2e` |

> [!note] Цвета тетромино
> Прописаны в `TetrominoData.Colors[]` — менять не нужно.
> HP-визуал слабых блоков работает автоматически (затемнение при уроне).

---

## Шаг 3 — Animator

`Assets → Create → Animator Controller → "Character"` → назначить на `Animator` компонент.

| Параметр | Тип | Источник |
|----------|-----|----------|
| `Speed` | Float | `UpdateAnimations()` |
| `Grounded` | Bool | `UpdateAnimations()` |
| `WallSlide` | Bool | `UpdateAnimations()` |
| `Jump` | Trigger | `PlayJump()` |
| `Attack` | Trigger | `PlayAttack()` |
| `HitStrong` | Trigger | `PlayHitStrong()` |

> [!note] Аниматор опционален
> `CharacterView` проверяет `_animator != null` перед каждым вызовом.
> Можно запустить без анимаций — всё будет работать.

---

## Шаг 4 — Настройки Inspector

### TetrisController

| Параметр | Значение | Что делает |
|----------|----------|-----------|
| Board Width | `10` | Ширина доски |
| Board Height | `20` | Высота доски |
| Cell Size | `1` | Размер ячейки в Unity единицах |
| Fall Interval | `1.0` | Секунд между шагами вниз |
| Soft Drop Mult | `0.08` | Ускорение при удержании ↓ |
| Block HP | `3` | HP каждого блока (3 удара = сломан) |

### Позиция персонажа

```
Персонаж стартует внутри доски у нижнего края:
  X = TetrisController.X + (BoardWidth / 2) * CellSize   ← центр
  Y = TetrisController.Y                                  ← дно
```

---

## Шаг 5 — Playtest

### Тетрис

- [ ] Фигуры спавнятся сверху и падают
- [ ] `← →` — движение, `↑ Z` — поворот, `↓` — ускорение, `Space` — hard drop
- [ ] Ghost-фигура отображается прозрачной
- [ ] Заполненная линия очищается и строки падают вниз

### Персонаж

- [ ] `A / D` — движение, `W` — прыжок
- [ ] Coyote Time — можно прыгнуть сразу после края блока
- [ ] Wall-slide — медленнее падает у стены
- [ ] Wall-jump — отталкивается от стены
- [ ] Персонаж **не** проходит сквозь падающую фигуру

### Взаимодействия

- [ ] `J / L` — удар: слабый блок темнеет → при HP = 0 исчезает
- [ ] Удар по сильному блоку — играет триггер `HitStrong`
- [ ] Crush — блок фиксируется на персонаже → победа Тетриса
- [ ] Персонаж набирает 30 блоков → победа Персонажа
- [ ] Очистка линий считается в счёт персонажа
- [ ] GameUI обновляет счётчик в реальном времени

---

## Шаг 6 — Баланс

| Параметр | По умолчанию | Где менять | Влияние |
|----------|-------------|------------|---------|
| `blocksToWin` | 30 | GameManager | Длина партии |
| `fallInterval` | 1.0 | TetrisController | Скорость тетриса |
| `blockHP` | 3 | TetrisController | Сколько ударов ломает блок |
| `jumpForce` | 12 | CharacterController2D | Высота прыжка |
| `maxSpeed` | 6 | CharacterController2D | Скорость бега |

---

← [[Tetris vs Hero]] · [[Роадмап]] · → [[Фаза 2 — Мультиплеер MVP]]
